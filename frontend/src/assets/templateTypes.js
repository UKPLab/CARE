// Keep in sync with the lists on the template model.
export const emailTemplateTypes = Object.freeze([1, 2, 3, 6, 7]);
export const otherTemplateTypes = Object.freeze([4, 5]);
export const allTemplateTypes = Object.freeze([...emailTemplateTypes, ...otherTemplateTypes]);
