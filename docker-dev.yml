version: "3"
services:
  postgres:
    ports:
      - ${POSTGRES_PORT}:5432
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
  rpc_test:
    ports:
      - ${RPC_TEST_PORT}:8080
  rpc_moodle:
    ports:
      - ${RPC_MOODLE_PORT}:8081
  rpc_pdf:
    ports:
      - ${RPC_PDF_PORT}:8082