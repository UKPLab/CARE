Coordinator
-----------

The coordinator component is a wrapper around form components. It offers a simple way to build a form to add/edit data in the backend.


Form
----
To build forms, you can use the form component. It offers a simple way to build forms in a consistent way only by passing a dictionary.

