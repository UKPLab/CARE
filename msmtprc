# /etc/msmtprc - used in the docker image to configure msmtp

defaults
auth           off
tls            off
syslog         on

# Relay all mail to the host machine's sendmail
account        default
host           host.docker.internal
port           25