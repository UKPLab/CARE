{
  "name": "assessment-score",
  "version": "1.0.0",
  "description": "Shared assessment score calculation for CARE frontend and backend",
  "main": "index.js",
  "scripts": {
    "test:module": "cross-env NODE_ENV=test jest --silent=false --forceExit --testTimeout=10000 --runInBand --collect-coverage --detectOpenHandles tests/assessment-score.test.js"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.3.0"
  }
}
